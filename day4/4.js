//Accept Number from user and display table of that number in table format 

    function generateTable(a) {
        for(let i=1;i<10;i++){
            console.log( i*a+" ");
        }
    }
    generateTable(5);

